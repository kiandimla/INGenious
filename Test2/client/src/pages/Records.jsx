import{useEffect,useState}from'react';import Layout from'../components/Layout';import Notice from'../components/Notice';import{api}from'../api/client';
export default function Records(){const[data,setData]=useState(null),[type,setType]=useState('sales'),[msg,setMsg]=useState('');useEffect(()=>{api('/api/reports/records').then(x=>setData(x.report)).catch(e=>setMsg(e.message))},[]);const rows=data?.[type]||[];const keys=rows[0]?Object.keys(rows[0]):[];return <Layout title="Records"><div className="toolbar"><select value={type} onChange={e=>setType(e.target.value)}><option value="sales">Sales</option><option value="deliveries">Deliveries</option></select></div><Notice message={msg}/><div className="table-wrap"><table><thead><tr>{keys.map(k=><th key={k}>{k}</th>)}</tr></thead><tbody>{rows.map((x,i)=><tr key={i}>{keys.map(k=><td key={k}>{x[k]??'—'}</td>)}</tr>)}</tbody></table></div></Layout>}
