import{useEffect,useRef,useState}from'react';import{Link,useNavigate}from'react-router-dom';import{useAuth}from'../context/AuthContext';import Notice from'../components/Notice';
export default function Login(){const[name,setName]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');const{user,login}=useAuth(),nav=useNavigate(),ref=useRef();useEffect(()=>ref.current?.focus(),[]);useEffect(()=>{if(user)nav('/home')},[user]);async function submit(e){e.preventDefault();try{await login(name,password);nav('/home')}catch(x){setError(x.message)}}return <div className="center-page"><form className="card login" onSubmit={submit}><h1>PhIMS</h1><p>Inventory Management System</p><input ref={ref} value={name} onChange={e=>setName(e.target.value)} placeholder="Username" required/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" minLength="3" required/><button>Log in</button><Link to="/forgot-password">Forgot password?</Link><Notice message={error}/></form></div>}
