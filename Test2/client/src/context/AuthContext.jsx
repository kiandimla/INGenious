import {createContext,useContext,useEffect,useState} from 'react';import{api}from'../api/client';
const C=createContext(null);export function AuthProvider({children}){const[user,setUser]=useState(undefined);useEffect(()=>{api('/api/auth/session').then(x=>setUser(x.user)).catch(()=>setUser(null))},[]);async function login(name,password){const x=await api('/api/auth/login',{method:'POST',body:JSON.stringify({name,password})});setUser(x.user)}async function logout(){await api('/api/auth/logout',{method:'POST'});setUser(null)}return <C.Provider value={{user,login,logout}}>{children}</C.Provider>}export const useAuth=()=>useContext(C);
